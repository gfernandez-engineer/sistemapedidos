apiVersion: apps/v1
kind: Deployment
metadata:
  name: deliveries-service
  namespace: food-ordering
  labels:
    app: deliveries-service
spec:
  replicas: 2
  selector:
    matchLabels:
      app: deliveries-service
  template:
    metadata:
      labels:
        app: deliveries-service
    spec:
      containers:
        - name: deliveries-service
          image: food-ordering/deliveries-service:latest
          ports:
            - containerPort: 8085
          env:
            - name: SPRING_DATASOURCE_URL
              valueFrom:
                secretKeyRef:
                  name: deliveries-db-secret
                  key: url
            - name: SPRING_DATASOURCE_USERNAME
              valueFrom:
                secretKeyRef:
                  name: deliveries-db-secret
                  key: username
            - name: SPRING_DATASOURCE_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: deliveries-db-secret
                  key: password
            - name: SPRING_KAFKA_BOOTSTRAP_SERVERS
              value: kafka:9092
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8085
            initialDelaySeconds: 60
            periodSeconds: 30
          resources:
            requests:
              memory: "256Mi"
              cpu: "250m"
            limits:
              memory: "512Mi"
              cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: deliveries-service
  namespace: food-ordering
spec:
  selector:
    app: deliveries-service
  ports:
    - port: 8085
      targetPort: 8085
  type: ClusterIP
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: deliveries-service-hpa
  namespace: food-ordering
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: deliveries-service
  minReplicas: 2
  maxReplicas: 5
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 70
